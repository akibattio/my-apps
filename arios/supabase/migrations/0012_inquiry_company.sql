-- ============================================================================
-- 0012 依頼に会社名(company)を追加
--  新規登録情報: 連絡先(電話/LINE 必須)・名前(必須)・メール(必須)・会社名(任意)。
--  会社名を name とは別項目で保持し、管理画面から編集できるようにする。
-- ============================================================================
alter table inquiries
  add column if not exists company text;
