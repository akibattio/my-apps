# OpenAI (gpt-image-1)
OPENAI_API_KEY=sk-...

# Google Cloud Vertex AI (Gemini 2.5 Flash Image = Nano Banana)
GOOGLE_CLOUD_PROJECT=your-gcp-project-id
GOOGLE_CLOUD_LOCATION=us-central1
# Run `gcloud auth application-default login` once, or set the path below:
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Output directory (default: ./output)
# BANNER_OUTPUT_DIR=./output
