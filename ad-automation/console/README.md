# console/ — 管理画面

チャットで作成したプロトタイプ `ad_ops_console.jsx` の配置先。
（概要 / クライアント別 / 接続ステータス / 費用・成果一覧 / 承認キュー / ヘルス&アラート表示）

## 状態
- ☐ `ad_ops_console.jsx` を配置（現在このリポジトリ未取り込み。チャット成果物を貼り付ける）。
- ☐ 実データ接続（バックエンド＋定期取得）は Phase 2 で着手（HANDOFF §8-7）。

## 実運用での接続設計（Phase 2）
- 裏側に MCP/API と定期取得（`routines/daily-check.md`）を接続。
- 表示するのは「取得済み数値」と「承認キュー（下書き）」。**適用操作は承認ゲート経由のみ。**
